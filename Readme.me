*If your PC has not installed Python, Install it first

*Create a ditectory and copy the python files to that directory.

*Save the Excel data file in the save directory created above.

*Run the cmd in the same directory and run the "Combiner.py" file in the directory.

*After the completion of the programe a excel file will appear in the directory as the final report.
(There will be two other reports separately for each vendor.)